# Changelog

## 0.1.0 (Unreleased)

Initial release with project scaffolding.

- Package structure with five core modules: data, tokenizers, models, training, eval.
- Configuration schema with YAML loading via OmegaConf.
- CLI skeleton with `train` and `benchmark` subcommands.
- CI/CD via GitHub Actions (lint, test, typecheck, release).
- Sphinx + MyST documentation skeleton.
